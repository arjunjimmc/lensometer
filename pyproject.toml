[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "lensometer-prototype"
version = "0.1.0"
description = "Mobile camera-based lensometer prototype"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
